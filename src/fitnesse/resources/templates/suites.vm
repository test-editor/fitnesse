{
  "suites": [
#foreach( $suite in $suites )
    {
      "name": "$suite.name"
#if(!$suite.testReferences.empty),
      "tests": [
#foreach( $testReference in $suite.testReferences  )
          "$testReference" #if( $foreach.hasNext ),#end
#end
		]
#end
    }#if( $foreach.hasNext ),#end
 #end
 	]
 }