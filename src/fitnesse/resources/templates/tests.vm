{
  "tests": [
#foreach( $test in $tests )
    {
      "name": "$test.name",
      "path": "$test.path",
      "metadata": [
#foreach( $metaDataTag in $test.metaDataTags )
       {
          "name": "$metaDataTag.name",
          "value": "$metaDataTag.value"
        }#if( $foreach.hasNext ),#end
#end
		]
    }#if( $foreach.hasNext ),#end
 #end
 	]
 }